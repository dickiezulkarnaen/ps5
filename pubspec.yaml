name: ps5
description: Playstastion 5 Games List.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  dio: ^4.0.6
  flutter_bloc:
  cached_network_image: ^3.2.3
  intl: ^0.18.0
  flutter_rating_bar: ^4.0.1
  photo_view: ^0.14.0
  logging: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
  - assets/images/games.png
